export default function ExampleQuery({text}) {
  return (
    <div className="mt-4 ">
      <p className="inline-block text-sm bg-white/30 text-white px-4 py-1 rounded-full">
        {text}
      </p>
    </div>
  );
}
